# BACKLOG — очередь будущих правок

> Наблюдённые, но ещё НЕ закрытые задачи (в осн. по простановке). Приоритет сверху вниз.
> Принцип: [[prefer-refusal-over-guess]] — при сомнении простановка ОТКАЗЫВАЕТ, не гадает.
> Живое состояние сессии — в `HANDOFF.md`.

> **🛡️ Боевая защита (0.10.17) — 4 гейта ПЕРЕД реальным кликом:**
> 1. **Судья `verifyPick`** (lib/bookers.cjs) — выбор == сигнал (тип/линия/сторона/кэф, без экспрессов). Ловит и будущие мис-селекты.
> 2. **oddsOk** — кэф Betano на странице не просел ниже допуска.
> 3. **Свежий fair (#4b)** — перевес ЕЩЁ есть по последнему опросу Pinnacle (≤1.5с); исчез/ниже порога → dry-run.
> 4. **verifyPlaced (#4)** — после клика подтверждаем приём БК (кнопка исчезла); не принято → `placed=false` + ERROR.
>
> **Охват боевого:** `valueLiveSports=[3]` — живьём ТОЛЬКО теннис (проверено); прочие спорты в боевом → dry-run (отладка идёт).
> Telegram-алерт на боевую ставку и на блок судьи. Дальше (сильнее) — читка купона обратно (слова Betano).

## 0. Находки боевой сессии 0.10.21 (разбор main-3.log, 2026-07-06) — СВЕЖЕЕ, часть про РЕАЛЬНЫЕ ДЕНЬГИ
Итог по логу: 14 строк `placed:true`. НО владелец сверил с историей Betano — **логи НЕ отражают реальность**
(см. 0c, это корень). Поэтому «14 поставлено» недостоверно; истина — только в истории Betano.
Найдено 4 проблемы: **0a ✅ + 0b ✅ сделаны (тесты 199)**; 0c ⏳ v1 (нужна боевая сессия для подтверждения); 0d — не баг.

> **🔴 ГЛАВНЫЙ ВЫВОД (0c): логам о простановке верить нельзя.** `verifyPlaced` угадывал приём по «исчезла ли
> кнопка BET NOW» → врал в ОБЕ стороны. Владелец нашёл руками: ставка есть на Betano, а в логе/ТГ «не поставлено»
> (и наоборот). Пока read-back по квитанции не подтверждён живой сессией — счётчики/лимиты/ТГ ненадёжны.

- **0a. Гард «обе стороны одного матча» — ✅ СДЕЛАНО (тесты).** Был баг: в `Taylor Fritz vs Alexander Bublik`
  поставлены ОБЕ победы (Фрица 1.98 И Бублика 2.40) — одна гарантированно проигрывает. `valuePlaceEventExtra`
  не запрещал встречную сторону.
  - **Фикс:** гард в `choosePlacement` (lib/valueplace.cjs), НЕЗАВИСИМ от лимитов dup/event/market. Ключ рынка
    = событие|st|kind|abs(линия); если та же (event+st+kind+|линия|) уже в работе на ДРУГОЙ стороне → отказ.
    Ловит ML1⟺ML2, Over⟺Under одной линии, +L⟺−L. Консервативно (блок даже если та сторона лишь пробована).

- **0b. Мис-селект «to win to» / условный тотал — ✅ СДЕЛАНО (тесты).** Был баг: ML-сигнал матчился на
  гейм-проп «… to win to N» (встретился 10×, поставился `Stefan Palosi to win to 15 @2.32`); а матчевый тотал —
  на условный «Under 41.5 … Score 3-0». Ни пул победителя, ни судья их не ловили.
  - **Фикс (lib/bookers.cjs):** хелпер `isProp` (`to win to`/`to win a set`) режет проп во ВСЕХ win/ML-пулах
    `pickOutcome` (484, 464, 502, 525); total-пул (436) режет условный тотал по `scoreOf===null`. Судья
    `verifyPick`: ML отклоняет гейм-проп, TOTAL отклоняет тотал со счётом («X-Y»).
  - **Осталось:** если ярлык условности («Score 3-0») живёт в ЗАГОЛОВКЕ секции, а НЕ в тексте кнопки — по тексту
    не поймать; это тот же контекст-гэп, что п.1 (нужна читка колонки/заголовка рынка).

- **0c. 🔴 verifyPlaced врёт в ОБЕ стороны — логи ≠ реальность (КРИТИЧНО, ПРАВИТСЯ).** Подтверждено сверкой
  владельца с историей Betano:
  - **Ложный МИНУС** (лог «не поставлено», а ставка ЕСТЬ): `Eduardo −5.5 2.15` (стр.1446) и
    `Under 41.5 2.10 / Score 3-0` (стр.962) — обе подтверждены владельцем на Betano. Ещё 2 «приём НЕ подтверждён»
    под подозрением: `+4.5 1.75` (стр.990), `+1.5 3.40` (стр.1462) — **владельцу сверить в истории Betano**.
    Всего таких `боевой НЕ поставил (verifyPlaced)` за сессию — 4.
  - **Ложный ПЛЮС** (лог/ТГ «поставлено», а ставки НЕТ): `Jiri Under 33.5` (стр.1858, `placed:true`+ушло в ТГ).
  - **Причина:** старый `verifyPlaced` = «исчезла ли кнопка BET NOW» (мигает при ре-рендере → врёт оба).
  - **Сделано в коде (main.cjs, требует боевой сессии для подтверждения):** признак приёма сменён на
    «поле суммы купона `input[id^="stakeInput"]` ИСЧЕЗЛО» (принято уводит купон в квитанцию; отклонённое
    оставляет выбор висеть) — разворачивает оба бага верно. Добавлен `captureBetslip` — снимок квитанции/купона
    в лог (`[диаг купон после клика]`) для сбора реальной разметки Betano.
  - **✅ READ-BACK v2 СДЕЛАН (0.10.27):** v1 («поле суммы исчезло») тоже дал ложные минусы — принятые ставки
    числились непоставленными (доказано логами 07-07: winnings 3,74=2×1.87 и 6,90=2×3.45 висели с CASH OUT
    после «не подтверждён»). v2: мультисет строк «Bet <stake> € Potential winnings <W> €» ДО клика vs ПОСЛЕ →
    появилась новая строка с нашей суммой = ПРИНЯТО (позитивное доказательство) + реальный кэф W/stake
    (realOdds → лог «ПОСТАВЛЕНО @реальный», ТГ). Не появилась → не поставлено + ERROR. Проверить боевой сессией.

- **0d. Дрейф фид→страница режет конверсию (не баг, потолок).** Из ~105 сигналов поставлено ~13; ~40%+
  испаряются на раннем кэф-гейте (`пропуск (кэф уехал до купона)`). Ограничение фида/сети, не дефект кода.

---

## 0e. 🎯 ЗАМЕР ПОДТВЕРЖДЁН (лог 2026-07-07 15:09–15:11, боевая 0.10.25): id фида == data-selnid кнопки.
**3/3 ✅** когда кнопка на странице: id найден И совпал с выбором бота (i=80✓, i=24✓, i=23✓).
**3× ✖ «не найден»** = кнопки реально НЕ БЫЛО (линия тотала сместилась в live: сигнал 19.5 ↔ на странице 20.5)
— честный отказ, id-схема безопаснее старого матчинга (тот мог схватить соседнюю линию).
**РЕШЕНИЕ ЗАФИКСИРОВАНО и СДЕЛАНО (0.10.26): выбор — ПРЯМОЙ клик по id.** `pickOutcome({selnid})`:
кнопка ищется по data-selnid == betanoSelId фида; кнопки нет → отказ БЕЗ текстового фолбэка (он и давал
мис-селекты). Старый матчинг — только для сигналов без id (вилки/Pinnacle). Судья/кэф-гейты/купон — страховки.
Плюс замер «спрятанной кнопки»: id не найден → раскрываем все [aria-expanded=false] → перечитываем → лог
`[selnid] … НАЙДЕН после раскрытия (пряталась)` / «не найден → исход снят». Тесты 202.
**Другие спорты:** главный блокер (текстовый мис-селект) снят — id не зависит от языка/формата кнопок.
Включение в боевой — после проверки dry-run других спортов на 0.10.26 (смотреть how=selnid в dry-run).

### (было) ЗАМЕР: прямая связка «фид → кнопка» по id (selnid) — шёл в 0.10.25
**Корень всех мис-селектов рынков:** бот читает ТЕКСТ кнопки (имя+кэф), а рынок кнопки («Winner» vs
«Game Winner (Set 2, Game 10)») в тексте НЕ живёт → выбор «ближайшим кэфом» хватает чужой рынок
(Alice Tubello: сигнал ML → встал Game Winner Set 2 Game 10).
**Находка:** у кнопок Betano есть `data-selnid` (id выбора, видно в [диаг betano ctx2]); в фиде у исхода
есть `eventId` («9949062375|284…») — первая часть того же формата. ГИПОТЕЗА: это ОДИН id.
**Замер (0.10.25):** скрап тащит selnid; сигнал несёт betanoSelId (bettingco→valueplace→pendingBet);
в простановке лог `[selnid-замер]`: «✅ СХЕМА РАБОТАЕТ» = id фида найден среди кнопок (+ совпал ли выбор бота).
**Если сработало → ЗАФИКСИРОВАТЬ и перевести pickOutcome на прямой клик по id** (кэф/имя — только
доп-сверка; судья по купону — страховка). Если нет → план Б: ярлык рынка из купона (slipText уже в логе
`[диаг купон: рынок]`) + судья по ярлыку.

## 0f. ✅ Склейка событий между БК: teamKey (0.10.27) — открывает Dota/CS/бейсбол
Замер фида 07-07: Dota (Esports World Cup) была в ОБОИХ плечах, пересечение 0 — «Team Yandex»≠«Yandex»,
«LGD Gaming»≠«LGD» по точным именам. Общего id событий между БК в фиде НЕТ (selnid решает только «какую
кнопку нажать» ВНУТРИ Betano) → склейка контор всегда именная. Фикс: `teamKey` (lib/bettingco.cjs) — скобочные
ники вон, шумовые токены (team/gaming/esports/fc…) вон; `gamesByPair`+`eventSync.flip` на teamKey. Тесты 203.
Примечание: кибер-футбол/кибер-баскет (H2H GG/eAdriatic симуляторы) у Pinnacle-плеча НЕТ вовсе — не чинится;
наст.теннис в Pinnacle-плече в замерах 0 live (наблюдать); амфутбол/НБА — от сезона (сент/окт), придут сами.
**Мировой бейсбол (0.10.28):** US-алиасы городов (la→los angeles, ny→new york…, спорные NO/MIN/DC не включены);
латам — исп. предлоги (de/del/y) в шум + сортировка токенов (порядок слов не важен: «Diablos Rojos del México»
== «Mexico Diablos Rojos»); NPB — SUBSET-фолбэк в matchEvents («Fukuoka SoftBank Hawks»↔«SoftBank Hawks»),
строго при ЕДИНСТВЕННОМ кандидате с обеих сторон (два «Giants» → отказ); sameTeam→eventSync.flip не врёт.
KBO-бренды (LG/KT/SSG/NC) не трогаем. Тесты 205. Ночной MLB-замер имён — подтвердить/дополнить алиасы по факту.

## 0g. 🔬 ИДЁТ: суточный мониторинг потенциала спорт×маркет (стартовал 2026-07-08 ~11:30 UTC)
Владелец ждёт ОТЧЁТ: все спорты × маркеты обеих контор — количество рынков, медиана маржи Pinnacle,
value-кандидаты ≥3/≥5%, несклеенные имена (для алиасов). Снимки каждые 10 мин, 24ч, nohup-процесс на маке.
**Файл данных:** `/private/tmp/claude-501/-Users-danidey27icloud-com-Downloads---------/a53eb0d4-3ad8-4bb8-88ab-651222d3c643/scratchpad/feed-potential.jsonl`
(скрипт `feed-monitor.cjs` рядом). Отчёт: сводка по часам/спортам/маркетам + вывод «что включать в боевой».
**Гипотезы на проверку по данным:**
- 🏀 Баскет: события склеиваются 5/5, но РЫНКИ отваливаются до расчёта — подозрение на eventSync
  (формат счёта по четвертям различается между БК → ложный «рассинхрон»). Если да — фильтр душит спорт, чинить.
- Dota: склейка работает (Falcons сошёлся), потери только на переименованиях (ZEDI↔Poor Rangers, BetBoom↔BB) —
  лечится ТОЧЕЧНЫМИ алиасами, копить по фактам из unmatched.
- Футбол: «Anjos do Ceu FC U20» ↔ «Associacao Anjos do Ceu» — U20-суффикс/приставки клубов, кейс для алиасов.
- Маржа-ориентиры: теннис ML 3.6% / форы-тоталы 4.2–5.1% (рабочие); кибер 7–9% (мутные); порог доверия —
  value ≳ маржа/2.

## 0h. ✅ Состояние на 11.07 (релизы 0.10.35–0.10.37)
- **Гейт «остаточного value»** (правило владельца): просадка кэфа не важна — ставим, пока цена×свежий fair−1
  ≥ порога панели. Ранний гейт, oddsOk и купонный гейт — все на остаточном value (вилочный путь — старый допуск).
- **eventSync v2**: слепой счёт (Pinnacle «0-0»/пусто при живом) и тик-допуск (баскет/волейбол/гандбол ±3,
  наст.теннис ±2) — не рассинхрон. Открыл Dota/Valorant/CS/волейбол/баскет. Теннис/футбол/бейсбол/хоккей строгие.
- **Боевой охват**: [3,1,29,31,32] — теннис+футбол+CS+LoL+Valorant (критерий 5+ чистых dry-run, диаг 09.07:
  Valorant 7/7, CS 5, LoL 5/5, футбол 5/6, 0 мис-селектов). Dota копит.
- **Маржа по спортам**: valueMarginBySport {sportType:доля} + 9 полей в панели. Авто-сохранение конфига (change).
- **Контролёр позднего приёма**: сверяет цель только на СТРАНИЦЕ её события (id в URL), WARN если посещали и
  нет; 2ч если не посещали. Подтверждений пока 0 — наблюдать лог.
- **Прокси**: v2-блок (CDP картинки/шрифты/видео, кэш жив) — выбор владельца после проб 0.10.30-34; тумблер в панели.
- **Открыто**: MLB-формат счёта (монитор под caffeinate -s копит ночь); слепые зоны Pinnacle-фида
  (наст.теннис/снукер/дартс/кибер-симы = 0 live стабильно); Dota-переименования (BetBoom↔BB) — точечные алиасы.

## 0i. 🧪 СОСТОЯНИЕ 12.07: аккаунт .bg порезан → диагностика + рандом-тест + пивот на lat.betano.com

### Аккаунт Betano .bg ПОРЕЗАН (блок ставок)
~70 боевых ставок за 06–11.07, итог −12€ (дисперсия, не отличима от шума при среднем value +5–6% на такой
выборке). Betano режет блоком ставок независимо от причины. Профиль наших ставок: 95% live-теннис ITF/
челленджеры, флэт 2€, гейм-тоталы/форы, машинный цикл ~21с.

### Диагностика «за что режут» — три выживших кандидата
Метод: коллега тоже БОТ (арбы, экзотика-футбол U19/U20, переменные некруглые стейки, сотни/день) — ЖИВ.
Его выживание ЭЛИМИНИРует: объём, круглосуточность, live-класс, шарп-профиль рынков, серии, дозаливы,
опережение линии (CLV как самостоятельную причину), «за профит». Остались ТРИ:
1. **Телеметрия НАШЕГО клиента** — `el.click()` даёт `isTrusted:false` (палится с 1-го клика); ноль
   загруженных картинок/шрифтов за месяц (блок ресурсов); прямые заходы по URL; метроном-тайминг.
2. **Содержимое** — «систематически на правильной стороне против Pinnacle» (CLV-скоринг).
3. **Анонимка** — прокси/отпечаток.

### Эксперимент для развязки: 2 VDS, оба НАШ бот (метод различия Милля)
- **Арм V** = наш бот + value/арб (реплика старого, но свежий прокси). **Арм R** = наш бот + рандом.
- Одновременно, тот же спорт/стейк/пейсинг, разные аккаунты/профили/прокси ОДНОГО класса. Разница ТОЛЬКО value↔рандом.
- **Дерево:** V выжил → виноват был старый ПРОКСИ. V умер как старый → прокси оправдан, смотрим R:
  R выжил → СОДЕРЖИМОЕ (CLV); R умер так же → НОСИТЕЛЬ (наш бот, телеметрия).
- Два VDS (не одна): single-instance-lock + риск линковки аккаунтов на общем хосте. На VDS общий только
  ключ фида → симметричный rate-limit, сравнение не портит.

### РАНДОМ-ТЕСТ РЕЖИМ — ✅ В КОДЕ (valueRandomTest), тесты 209/209, НЕ РЕЛИЗНУТ (владелец: «не собирай»)
Ставит СЛУЧАЙНЫЕ исходы (полная вселенная scan threshold −1, обе стороны), считает/логирует value/вилку/маржу,
но НЕ фильтрует по ним. Пейсинг СЕРИЯМИ (5–6 подряд по 5–45с, между сериями 3–5.5ч, ≤valueMaxPerDay/сут).
Полоса кэфов = поля панели «Кэф от/до» (footprint как у value-арма). Тумблер 🎲 в панели.
- **РАБОТАЮТ** (носитель/безопасность): selnid-клик, судья verifyPick, судья-купон, квитанция/read-back+
  реальный кэф+контролёр позднего приёма, анти-экспресс, спорт-охват, суточный лимит, дедуп, пейсинг.
- **ВЫКЛ** (value-фильтры): порог value, маржа, свежий fair, остаточный value, oddsOk(=true), гейт «кэф уехал»,
  requireArb, гард обе-стороны, лимиты матч/маркет.

### ПИВОТ на lat.betano.com (Betano Latam) — ✅ КОД СДЕЛАН (тесты 210/210), НЕ РЕЛИЗ
Владелец: id событий ТЕ ЖЕ, кэфы те же, английский есть → selnid/судья/ярлыки работают как есть.
Осталось (config): Octo-профиль на lat.betano.com + латам-прокси + аккаунт + URL конторы → lat.betano.com;
стейк под валюту lat (песо ~2000 / USD ~2).
**3 правки кода — СДЕЛАНЫ (заодно чинят любой домен/валюту):**
1. ✅ `betanoTarget` (lib/bookers.cjs) — универсальный: любой `betano.*` хост из настроек рерайтит на него же
   (был только `betano.bg`). Не-betano/notbetano → null (граница метки). Тест обновлён.
2. ✅ `SLIP_BETS_JS` (main.cjs) read-back — валюто-агностик: `Bet <сумма> [^\d]{0,6} … Potential winnings <W>`,
   символ валюты не фиксируем (проверено €/$/CLP/R$). Иначе на не-€ КАЖДАЯ ставка числилась бы «не принята».
3. ✅ Анти-экспресс счётчик вынесен в `countSlipSelections` (lib/vilka.cjs, +тест): режет денежную сводку/кнопку
   и валютные символы, ТОЛЬКО потом считает кэфы «X.YZ». Иначе на dot-валюте (lat: $2.00) сумма сошла бы за
   лишний выбор → overcount → купон блокируется (fail-safe, но lat не поставил бы НИЧЕГО).
НЕ нужно: переписывать простановку (id совпадают), языковой слой (англ есть).
⚠️ ЕДИНСТВЕННОЕ допущение: на lat/en кэфы даются ТОЧКОЙ (1.85), как на .bg. Подтвердить одним реальным дампом
купона lat («Снять купон» на live-событии) ДО боевого — заодно проверить формат валюты/ярлыки.

## 1. Гейм-фора: линия НЕ в тексте кнопки (mode A) — САМОЕ ЧАСТОЕ
Диагностика (`[диаг betano ctx] ... ⇐ ?`) показала: у голых кэф-кнопок гейм-форы линия **не в предках** —
лежит в СОСЕДЕ / заголовке колонки. Обход вверх её не находит → пул пуст → отказ. За сессию 17:16–17:50
**все 9 отказов** — именно гейм-форы (`AH…геймы`). Значит бо́льшую часть SPREAD-value мы не ставим.
- **Что сделать:** в скрапере `selectLegOutcome` тянуть линию для голой кэф-кнопки из строки/колонки рынка
  (не только предки — соседи/заголовок), собрать `{i,id,text,line}`; `pickOutcome` матчит по этой `line`.
- **Осторожно:** знак (−/+) привязать к правильной кнопке пары — иначе не та сторона (в боте — недопустимо).

## 2. ML по голому кэфу — ✅ СДЕЛАНО (0.10.15)
Было: `ML A|1` → голая «2.22»/«3 1.95» (`how=name`) вместо игрока. Фикс: для ПОБЕДЫ безымянные кнопки в пуле
не держим (только именованные нашей стороны или счёт) → голый кэф/токен-мусор больше не обыгрывает игрока
по близости кэфа. Тест добавлен. _Остаток под наблюдением:_ возможная не-та-сторона по имени (Luca vs Lorenzo)
— matchesOur уже роняет чужого игрока; если всплывёт снова — разобрать отдельно.

## 3. Персональные тоталы (запрос владельца)
Сейчас фид ОТСЕКАЕТ персональные тоталы («Игрок Games Won Over N»), чтобы они не сталкивались с матчевым
тоталом по ключу `st|kind|param` (иначе de-vig сравнил бы разные рынки → ложный value). Но:
- **(a) простановка:** на странице Betano персональный тотал «Игрок Over N» (БЕЗ «and») пока может совпасть с
  сигналом матчевого тотала. Отсечь в `pickOutcome` по имени игрока в тексте кнопки (комбо с «and» уже режем).
- **(b) фид (опционально):** поддержать персональные тоталы как ОТДЕЛЬНЫЙ рынок — ключ с именем игрока
  (`st|TOTAL|param|player`), чтобы не было коллизии и можно было ловить value и на них.

## 4. verifyPlaced в value-пути — ✅ СДЕЛАНО (0.10.17)
После боевого клика `placeBet` зовёт `verifyPlaced` (кнопка постановки исчезла = принято); не подтвердил →
`placed=false` + ERROR-лог «проверь купон вручную». Больше не оптимистичный `placed=true`.

## 4b. Пересчёт value по СВЕЖЕМУ fair перед боевым кликом — ✅ СДЕЛАНО (0.10.17)
В `runValueCycle` перед `placeBet`: `valueEngine.scan()` (данные ≤1.5с, движок опрашивает обе БК в фоне) → ищем наш
сигнал; исчез или `value < порога` → боевой ОТМЕНЁН, падаем в dry-run + WARN. Ловит уход Pinnacle за время открытия
события (oddsOk смотрит только Betano). На ошибке скана — тоже отмена (fail-safe). Форс-poll (вариант 2) не делаем.

## 5. Верификация очистки купона по betslip-count
`deselectLeg` логирует КЛИК снятия, но не подтверждает, что выбор реально ушёл из купона (нет селектора
betslip-элемента у Betano). См. [[log-verified-not-intended]].
- **Что сделать:** найти селектор строки купона Betano → перечитывать число элементов до/после → лог по факту.

## 6. Cookie/TCF-оверлей перекрывает купон — В РАБОТЕ (0.10.16)
Симптом (лог 20:23–20:26): ~8 dry-run с `поле суммы не найдено — исход не в купоне`, в кнопках видно
`Allow All`/`Reject All`/`Confirm My Choices` (это `<button>`), оверлей всплывает ПОЗДНО (на чтении суммы).
Правка 0.10.15 (чистка bidi) НЕ помогла. 0.10.16: CONSENT_JS агрессивнее (reject all→strict→confirm, div/span
по точному тексту, многошаговый «Confirm My Choices») + диагностика «видел кандидатов, но не кликнул» +
переклик исхода, если купон не наполнился. **Проверить на dry-run: пропали ли `поле суммы не найдено`.**

## ✅ Сделано
- **0.10.16 (в коде)** — 🛡️ судья `verifyPick` (боевой гейт + Telegram-алерт); cookie п.6 (агрессивный CONSENT_JS
  + переклик исхода + диагностика); тесты 196.
- **0.10.15** — ML голый токен/кэф не выбирается как победитель (п.2, ✅ подтверждено логом); ретрай при
  0 кнопок исходов (навигация таймаутнула → перезагрузка + ожидание + повторное чтение).
  _Cookie-оверлей в 0.10.15 НЕ починился (bidi-чистки мало) → переехал в п.6._
